# Grafana가 시작할 때 Prometheus를 데이터소스로 자동 등록한다.
# UI에서 손으로 설정하면 재현이 안 되므로 파일로 남긴다.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
